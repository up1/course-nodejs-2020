{
  "name": "demo-npm",
  "version": "1.0.0",
  "description": "``` $npm init $npm install validator --save ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "validator": "^13.7.0"
  }
}
